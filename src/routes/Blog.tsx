const Blog = () => {
  return (
    <div>
      {`
      Steps followed to setup this personal portfolio:
      1. followed create react app as a starter https://create-react-app.dev/ using the type script template 
      2. hosted with digital ocean apps which easily wraps this app in a contianer and created a yaml file for you https://www.digitalocean.com/
      3. added react router for code splitting


      TODO:
      1. add more lint rules
      2. configure prettier
      3. enforce tests and lint rules with husky
      4. add material ui
      `}
    </div>
  );
};

export default Blog;
